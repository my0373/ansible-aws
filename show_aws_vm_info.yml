---
- name: Dump the groups
  hosts: localhost
  connection: local
  tasks:
    - name: print names
      debug: msg="{{ item }}"
      with_items: groups['tag_Name_webserver']
      tags:
      - getprod